#include "main.h"

/**
*print_char - return characters
*/


int print_char(char p_char, 
              char buffer[], 
              int *buf_index)

{

    return (add_to_buffer(p_char, buffer, buf_index));

}
